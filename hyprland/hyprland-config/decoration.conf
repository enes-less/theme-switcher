# ======================
# DECORATION (MINIMAL)
# ======================
# Do not add more unless you are sure. Theme switcher provides more decorations
# in generated-theme.conf in .config/hypr

decoration {
    active_opacity = 0.9
    inactive_opacity = 0.85
}